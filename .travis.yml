language: python
python:
  - "3.3"
virtualenv:
  system_site_packages: true
install:
  - pip install coveralls
  - pip install -r requirements.txt
script:
  - python setup.py
  - coverage run --include=./*.py -m unittest discover -s wol4getmac.py
after_success:
  coveralls
